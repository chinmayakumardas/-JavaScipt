{
    //Dot Notation: Used to access properties of an object.
const person = { name: 'John', age: 30 };
console.log(person.name); // outputs "John"
}
{
    //Bracket Notation: Used to access properties of an object using a string.
const person = { name: 'John', age: 30 };
console.log(person['name']); // outputs "John"
}
{
//JSON Notation: Used to represent data in a human-readable format.
const person = {
    "name": "John",
    "age": 30,
    " occupation": "Developer"
  };

// example of JSOn

//go here https://dummyjson.com/products/


}